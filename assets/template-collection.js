const selectors = {};
class templateCollectionJS {
    constructor() {
        this.init();
    }

    init(){
        // JS code for collection features
    }
}

typeof templateCollectionJS !== 'undefined' && new templateCollectionJS();

 